import { EElement, ERails } from "src/types/enums";

export const elements = [
  EElement.Bowl,
  EElement.Full,
  EElement.Ledge,
  EElement.Mini,
  EElement.Pool,
];

export const rails = [ERails.Down, ERails.Rainbow, ERails.Round, ERails.Square];
