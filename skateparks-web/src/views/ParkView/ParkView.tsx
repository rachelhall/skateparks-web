import React from "react";

import "./ParkView.scss";

interface IProps {

}

export const ParkView: React.FC<IProps> = (props) => {
    const { } = props;

    return (
        <div className="ParkView">

        </div>
    );
};

export default ParkView;
